let ball;




function setup() {
  // Set up the canvas
  new Canvas(800, 400);
  background(250); //background color
  
  ball = new Sprite();
  ball.x = 100;
  ball.y = 200;
  ball.diameter = 100;
  ball.color = "brown";



}

function draw() {
  // fill("skyblue")
  // stroke("pink")
  // strokeWeight(10)

  // circle(30, 30, 50)
  // rect(50, 50, 100, 200)
  // rect(150, 250, 100, 100)
}